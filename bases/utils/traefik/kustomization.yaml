apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: utils

commonAnnotations:
  k3s.reynn.net/base: traefik

resources:
  - default.tls.options.yaml
  - default.tls.store.yaml
  - redirect.to.secure.yaml

generators:
  - helm.chart.yaml

patchesJson6902:
  - path: crb.ns.json-patch.yaml
    target:
      group: rbac.authorization.k8s.io
      kind: ClusterRoleBinding
      name: traefik
      version: v1
